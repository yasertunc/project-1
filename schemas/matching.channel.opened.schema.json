{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://www.fellowus.com/schemas/matching.channel.opened.schema.json",
  "title": "matching.channel.opened",
  "type": "object",
  "allOf": [
    {
      "$ref": "https://www.fellowus.com/schemas/common.json"
    }
  ],
  "properties": {
    "eventName": {
      "const": "matching.channel.opened"
    },
    "eventVersion": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+$"
    },
    "eventTime": {
      "type": "string",
      "format": "date-time"
    },
    "traceId": {
      "type": "string"
    },
    "requestId": {
      "type": ["string", "null"]
    },
    "matchId": {
      "type": "string"
    },
    "channelId": {
      "type": "string"
    },
    "channelType": {
      "type": "string",
      "enum": ["dm", "group"]
    },
    "userId": {
      "type": ["string", "null"]
    },
    "participants": {
      "type": ["array", "null"],
      "items": {
        "type": "string"
      }
    },
    "intent": {
      "type": ["string", "null"],
      "enum": ["incident", "route", "social", "coach", null]
    },
    "platform": {
      "type": ["string", "null"],
      "enum": ["ios", "android", "web", null]
    },
    "appVersion": {
      "type": ["string", "null"]
    },
    "locale": {
      "type": ["string", "null"]
    },
    "region": {
      "type": ["string", "null"]
    },
    "privacyTier": {
      "type": ["string", "null"],
      "enum": ["anon", "low", "high", null]
    },
    "latencyMs": {
      "type": "integer",
      "minimum": 0
    }
  },
  "required": [
    "traceId",
    "channelType",
    "eventVersion",
    "channelId",
    "latencyMs",
    "eventName",
    "matchId",
    "eventTime"
  ]
}
