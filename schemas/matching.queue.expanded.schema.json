{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fellowus.com/schemas/matching.queue.expanded.schema.json",
  "title": "matching.queue.expanded",
  "type": "object",
  "allOf": [
    {
      "$ref": "https://fellowus.com/schemas/common.json"
    }
  ],
  "properties": {
    "eventName": {
      "const": "matching.queue.expanded"
    },
    "eventVersion": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+$"
    },
    "eventTime": {
      "type": "string",
      "format": "date-time"
    },
    "traceId": {
      "type": "string"
    },
    "requestId": {
      "type": "string"
    },
    "matchId": {
      "type": ["string", "null"]
    },
    "channelId": {
      "type": ["string", "null"]
    },
    "channelType": {
      "type": ["string", "null"],
      "enum": ["dm", "group", null]
    },
    "userId": {
      "type": ["string", "null"]
    },
    "participants": {
      "type": ["array", "null"],
      "items": {
        "type": "string"
      }
    },
    "intent": {
      "type": ["string", "null"],
      "enum": ["incident", "route", "social", "coach", null]
    },
    "platform": {
      "type": ["string", "null"],
      "enum": ["ios", "android", "web", null]
    },
    "appVersion": {
      "type": ["string", "null"]
    },
    "locale": {
      "type": ["string", "null"]
    },
    "region": {
      "type": ["string", "null"]
    },
    "privacyTier": {
      "type": ["string", "null"],
      "enum": ["anon", "low", "high", null]
    },
    "expansionAttempt": {
      "type": "integer",
      "minimum": 0
    },
    "newRadiusKm": {
      "type": "number",
      "minimum": 0
    },
    "etaSeconds": {
      "type": "integer",
      "minimum": 0
    },
    "ruleId": {
      "type": "string"
    }
  },
  "required": [
    "traceId",
    "requestId",
    "eventVersion",
    "newRadiusKm",
    "expansionAttempt",
    "etaSeconds",
    "ruleId",
    "eventName",
    "eventTime"
  ]
}
