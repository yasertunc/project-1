# ADR 0001 — Matching API Contract

- **Context:** Client/server contract stability needed.
- **Decision:** OpenAPI 3.1 (yaml) in `openapi/`, TS types via `openapi-typescript`.
- **Status:** Accepted.
- **Consequences:** Single source of truth; client generation and validation in CI.

