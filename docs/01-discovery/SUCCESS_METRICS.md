# Success Metrics (v1)

- Queue wait_time_ms p95 < 45, median < 20
- match.success_rate > 35%
- match.timeout_rate < 20%
- acceptance.response_time_ms p95 < 10s
- channel.open_latency_ms p95 < 2s
- ghost_rate trending down
- report.follow_up_rate tracked

